const items = ['Milk', 'Apple', 'Honey', 'Bread', 'Plate'];
let htmlBody = '';

numbers.forEach (item=> {
    htmlBody +=`<div><lable>${item}</lable></div><br /> `
});


const numbers = [0,1,2,3,4,5,6];

const afterNumberARR =numbers.map((number) => {
    return number - number ;
}

)